#include <cstdint>
#include <vector>

#include "sum-of-array.hpp"

namespace examples {

/**
 * @brief Compute the sum of a vector. Take in mind that the entries of the
 * vector are possibly very large.
 *
 * @param a input vector
 * @return int32_t sum of the entries of the input vector
 */
int32_t sumOfArray(const std::vector<int32_t>& a) { return 0; }

}  // namespace examples
